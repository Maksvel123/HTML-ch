import "./globals.css";

export const metadata = {
  title: "Email Template Checker",
  description: "AI-based template validator",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}